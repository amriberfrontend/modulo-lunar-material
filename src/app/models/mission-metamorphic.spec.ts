import { MissionMetamorphic } from './mission-metamorphic';

describe('MissionMetamorphic', () => {
  it('should create an instance', () => {
    expect(new MissionMetamorphic()).toBeTruthy();
  });
});
